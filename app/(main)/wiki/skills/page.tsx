import SkillsCodex from "@/components/wiki/SkillsCodex";

// Renders the wiki skills page view component.
// Returns the JSX element hierarchy for the page view.
export default function WikiSkillsPage() {
  return <SkillsCodex />;
}
